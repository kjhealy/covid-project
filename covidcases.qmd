---
title: "Covid Case Sample"
format: html
---

## Setup

```{r}
library(tidyverse)
```

## Read in the data


## Figtest

```{r}
mtcars |> 
  ggplot(mapping = aes(x = hp, 
                       y = mpg)) + 
  geom_point() + 
  geom_smooth() + 
  labs(title = "This is the plot I want")
```

